//
//  Array.swift
//  CaverSwift
//
//  Created by won on 2021/05/25.
//

import Foundation

public class StaticArray1: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray2: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray3: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray4: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray5: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray6: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray7: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray8: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray9: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray10: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray11: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray12: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray13: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray14: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray15: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray16: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray17: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray18: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray19: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray20: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray21: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray22: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray23: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray24: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray25: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray26: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray27: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray28: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray29: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray30: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray31: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}

public class StaticArray32: Type {
    init(_ value: TypeArray) {
        super.init(value)
        self.rawType = .FixedArray(value.subRawType, self.size)
    }
}
