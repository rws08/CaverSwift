//
//  Array.swift
//  CaverSwift
//
//  Created by won on 2021/05/25.
//

import Foundation

public class StaticArray1: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 1))
    }
}

public class StaticArray2: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 2))
    }
}

public class StaticArray3: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 3))
    }
}

public class StaticArray4: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 4))
    }
}

public class StaticArray5: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 5))
    }
}

public class StaticArray6: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 6))
    }
}

public class StaticArray7: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 7))
    }
}

public class StaticArray8: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 8))
    }
}

public class StaticArray9: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 9))
    }
}

public class StaticArray10: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 10))
    }
}

public class StaticArray11: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 11))
    }
}

public class StaticArray12: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 12))
    }
}

public class StaticArray13: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 13))
    }
}

public class StaticArray14: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 14))
    }
}

public class StaticArray15: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 15))
    }
}

public class StaticArray16: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 16))
    }
}

public class StaticArray17: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 17))
    }
}

public class StaticArray18: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 18))
    }
}

public class StaticArray19: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 19))
    }
}

public class StaticArray20: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 20))
    }
}

public class StaticArray21: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 21))
    }
}

public class StaticArray22: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 22))
    }
}

public class StaticArray23: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 23))
    }
}

public class StaticArray24: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 24))
    }
}

public class StaticArray25: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 25))
    }
}

public class StaticArray26: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 26))
    }
}

public class StaticArray27: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 27))
    }
}

public class StaticArray28: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 28))
    }
}

public class StaticArray29: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 29))
    }
}

public class StaticArray30: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 30))
    }
}

public class StaticArray31: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 31))
    }
}

public class StaticArray32: StaticArray {
    public init(_ values: [ABIType]) {
        super.init(values, .FixedArray(type(of: values.first!).rawType, 32))
    }
}
